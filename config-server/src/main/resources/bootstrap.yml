spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/muditshukla3/config-server-repository
          clone-on-start: true
          default-label: master
    fail-fast: true